[pytest]
addopts = -m "not e2e and not local"
markers =
    e2e: end-to-end tests requiring live SPCS cluster (deselect with '-m "not e2e"')
    local: local integration tests using docker compose (deselect with '-m "not local"')
